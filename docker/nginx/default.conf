# server {
#     listen 80;
#     index index.php index.html;
#     server_name localhost;
#     root /var/www/public;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass application:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }

#     location ~ /\.ht {
#         deny all;
#     }
# }

# server {
#     listen 80;
#     # Usaremos /var/www/html como o diretório raiz.
#     root /var/www;
#     index index.php;

#     access_log /dev/stdout;
#     error_log /dev/stderr;

#     location / {
#         try_files $uri $uri/ =404;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass   application:9000;
#         # Garante que o Nginx passe o caminho correto para o PHP-FPM
#         fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
# }

server {
    listen 80;
    server_name localhost; # Or localhost

    root /var/www; # Path to your PHP project files

    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass application:9000; # 'php-fpm' is the service name of your PHP-FPM container
        fastcgi_index index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}
